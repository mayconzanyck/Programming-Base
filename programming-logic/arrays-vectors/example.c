/*
#include <stdio.h>

int main() {
    int numeros[5] = {1, 2, 3, 4, 5};

    // Exibindo os valores do array
    for (int i = 0; i < 5; i++) {
        printf("%d ", numeros[i]);
    }

    return 0;
}
*/